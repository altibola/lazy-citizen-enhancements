# Normalize line endings to LF in the repository; checkout as-is on Windows.
* text=auto eol=lf

# Star Citizen INI files MUST have CRLF line endings (Windows standard)
*.ini text eol=crlf

# Binaries — never touch line endings.
*.exe binary
*.dll binary
*.pkl binary
